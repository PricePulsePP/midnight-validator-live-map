{
  "name": "midnight-validator-live-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "refresh": "node scripts/refresh-telemetry.mjs",
    "test": "node --test",
    "validate": "node scripts/validate-snapshot.mjs"
  },
  "dependencies": {
    "cobe": "^2.0.1"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  },
  "engines": {
    "node": ">=22"
  }
}
